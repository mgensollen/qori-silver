// Optional: set this when the API is hosted elsewhere (e.g. Render/Fly/Vercel).
// Example: window.QORI_API_BASE = 'https://your-backend.example.com';
window.QORI_API_BASE = 'https://qori-silver-api.onrender.com';

// Google Analytics 4 — create at https://analytics.google.com → Admin → Data streams.
// Paste your Measurement ID (starts with G-). Leave blank to disable tracking.
window.QORI_GA_ID = 'G-XL6ZL4NTK8';

